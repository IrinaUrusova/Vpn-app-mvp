# Android client skeleton

Текущий статус (шаг 1 UI):
- ✅ Экран `HomeScreen` в бренде AURA (dark + purple/cyan gradient)
- ✅ Кнопки: "Подключить/Отключить" и "Обновить конфиг"
- ✅ Подключён API-контракт `GET /v1/vpn/config`
- ✅ Retrofit интерфейс и фабрика клиента

## Что осталось до первого APK
1. Добавить `build.gradle` (Compose + Retrofit + Coroutines).
2. Создать `MainActivity` и связать `HomeScreen`.
3. Реализовать ViewModel с вызовом `AuraApi.getConfig(userId)`.
4. Вывести полученный конфиг в экран/хранилище.

## Step 2 complete (runnable module scaffold)
- ✅ Added Gradle project files (`settings.gradle.kts`, root/app `build.gradle.kts`)
- ✅ Added `AndroidManifest.xml` + INTERNET permission
- ✅ Added `MainActivity` with API fetch action wired to button
- ✅ `onFetchConfig` calls `GET /v1/vpn/config?user_id=ira-android`
- ✅ Toast feedback for success/error

## Step 3 complete (ViewModel + UI state)
- ✅ Added `HomeViewModel` + `HomeUiState`
- ✅ Home screen now shows API status (`idle/loading/ok/error`)
- ✅ Home screen shows config preview after fetch
- ✅ Fetch button is disabled while loading

## Step 4 complete (Recovery action)
- ✅ Added API method `POST /v1/vpn/reissue`
- ✅ Added button `Починить подключение`
- ✅ Wired recovery to ViewModel `reissue()`
- ✅ UI now updates config preview with `new_config` after successful reissue

## Step 5 prep (build pipeline)
- ✅ Added Gradle Wrapper skeleton (`gradlew`, `gradle/wrapper/gradle-wrapper.properties`)
- ⚠️ `gradle-wrapper.jar` still required (generated by `gradle wrapper`)
- ✅ Added `local.properties.example`
- ✅ Added GitHub Actions workflow to build debug APK artifact
